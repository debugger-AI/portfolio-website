{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd react-app && npm install && npm run build && cd .. && mkdir -p dist && cp -r react-app/dist/* dist/",
    "deploy": "echo 'Deployment handled by Cloudflare Pages'"
  }
}

