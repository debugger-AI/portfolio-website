{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd react-app && npm install && npm run build && cd .. && cp -r react-app/dist dist 2>/dev/null || xcopy /E /I /Y react-app\\dist dist\\"
  }
}

